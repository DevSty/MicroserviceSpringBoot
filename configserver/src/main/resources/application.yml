server:
  port: 8090
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/DevSty/MicroserviceSpringBoot
          search-paths: config-data/default